# Optional Concepts Revision

Present confidently:

- Zod request validation: `validateRequest.ts`, feature validation schemas.
- Backend authorization: `auth.middleware.ts`, `access.service.ts`.
- Prisma ORM: schema, repositories, migrations.
- Transactions: onboarding, appointment creation, queue sync/reorder.
- SQL indexes/constraints: schema `@@index`, `@@unique`, active slot unique migration.
- Deterministic no-show assistance: `prediction.service.ts`.
- Responsible AI boundary: advisory, explainable, human-controlled.
- Loading/error/success UI: shared feedback components and feature tests.

Prepare before presenting:

- current test status: run test commands first
- deployment: record live URLs/SHAs first
- responsive/accessibility claims: capture runtime evidence first

Do not claim yet:

- rate limiting
- browser E2E
- monitoring
- Redis/WebSockets/jobs/payments
- trained ML, LLM, RAG, agents
